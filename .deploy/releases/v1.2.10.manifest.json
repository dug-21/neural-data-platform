{
  "$schema": "../../schemas/manifest.schema.json",
  "version": "1.0",
  "release_version": "1.2.10",
  "description": "Fix column name mismatch: strip primary stream alias prefix for embeddings, prepend for prediction SQL",
  "changes": [
    {"type": "container", "target": "ndp-intelligence", "action": "build"},
    {"type": "container", "target": "ndp-intelligence", "action": "restart"}
  ]
}
