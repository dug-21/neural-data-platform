{
  "$schema": "../../schemas/manifest.schema.json",
  "version": "1.0",
  "release_version": "1.2.8",
  "description": "Fix PgVectorEngine block_on panic: use block_in_place for sync trait methods called from async context",
  "changes": [
    {"type": "container", "target": "ndp-intelligence", "action": "build"},
    {"type": "container", "target": "ndp-intelligence", "action": "restart"}
  ]
}
