{
  "$schema": "../../schemas/manifest.schema.json",
  "version": "1.0",
  "release_version": "1.2.1",
  "description": "Fix Pi production deployment: add ndp-intelligence service to deploy/pi/docker-compose.yml",
  "changes": [
    {"type": "container", "target": "ndp-intelligence", "action": "build", "no_cache": true},
    {"type": "container", "target": "ndp-intelligence", "action": "restart"}
  ]
}
