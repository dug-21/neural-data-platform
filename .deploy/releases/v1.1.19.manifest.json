{
  "$schema": "../schemas/manifest.schema.json",
  "version": "1.0",
  "release_version": "1.1.19",
  "description": "BUG-004: Diagnostic logging for Bronze memory leak investigation — RSS, accumulator, WAL size tracking + malloc_trim after Polars writes",
  "changes": [
    {
      "type": "container",
      "target": "air-quality-app",
      "action": "build"
    },
    {
      "type": "container",
      "target": "air-quality-app",
      "action": "restart"
    }
  ]
}
