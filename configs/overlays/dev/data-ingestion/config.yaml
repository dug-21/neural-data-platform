# Development overlay for data-ingestion
logging:
  level: debug
  
sources:
  synthetic:
    enabled: true
    interval: 1  # Faster in dev
  polygon:
    enabled: false  # No external APIs in dev
  alpha_vantage:
    enabled: false
    
monitoring:
  metrics:
    enabled: false  # Disable metrics in dev