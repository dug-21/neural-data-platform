# Development overlay for config-store
logging:
  level: debug
  
git:
  sync_interval: 60  # More frequent in dev
  
validation:
  strict_mode: false  # Relaxed validation in dev
  
monitoring:
  metrics:
    enabled: false  # Disable metrics in dev