apiVersion: 1
datasources:
  - name: NDP-DuckDB
    type: motherduck-duckdb-datasource
    uid: duckdb-ndp
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      # Path to DuckDB database file
      path: /duckdb/neural_platform.db
