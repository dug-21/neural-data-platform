apiVersion: 1
datasources:
  - name: NDP-SQLite
    type: frser-sqlite-datasource
    uid: duckdb-ndp
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      # Path to SQLite database exported from DuckDB
      path: /duckdb/grafana.db
