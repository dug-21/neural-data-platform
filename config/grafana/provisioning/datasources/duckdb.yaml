apiVersion: 1
datasources:
  - name: DuckDB
    type: motherduck-duckdb-datasource
    uid: duckdb-ndp
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      # Path to DuckDB database with Silver layer views
      path: /data/duckdb/neural_platform.db
