# Proposed Actions — NDP Dev Automation

Synthesized from research reports 01-06. Each item is a discrete workable unit.

## P0 — This Week (quick wins, high impact)

- [ ] **ACT-001**: Resolve `claude-flow swarm init` contradiction — Remove CLI references from `ndp-scrum-master.md` (line 34) and `prompt-check.sh` (lines 36, 60); all should say MCP `hive-mind_init` only [Source: 01]
- [ ] **ACT-002**: Fix TeamCreate reference in swarm-run — Remove "TeamCreate or parallel Tasks" from `swarm-run/SKILL.md` line 16 to match swarm-protocol.md [Source: 01]
- [ ] **ACT-003**: Add agent timeout protocol — Define max wait time per agent (e.g., 10 min) in `swarm-protocol.md`; coordinator marks task failed and proceeds with available results on timeout [Source: 01, 06]
- [ ] **ACT-004**: Install `just` and create Justfile — Replace broken Makefiles with working recipes: `just check`, `just test`, `just lint`, `just integration`, `just clean` [Source: 03]
- [ ] **ACT-005**: Lightweight pre-commit hook — `cargo fmt --check` + anti-stub grep on staged `.rs` files only; install via `just install-hooks` [Source: 03]
- [ ] **ACT-006**: Delete or archive legacy Makefiles — Remove `Makefile` and `Makefile.v2` (reference non-existent Neural Trader binaries) [Source: 03]
- [ ] **ACT-007**: Fix `.cargo/config.toml` — Remove `autonomous-platform` references; gate `instrument-coverage` behind env var [Source: 03]
- [ ] **ACT-008**: Test count baseline in `/validate` — After `cargo test --workspace`, compare against stored baseline (e.g., `tests/baseline-count.txt`); warn on regression [Source: 03]
- [ ] **ACT-009**: Flaky test manifest — Create `tests/flaky.txt` listing 5 wiremock tests + `acceptance_partition_structure`; `/validate` excludes or separately reports these [Source: 03]
- [ ] **ACT-010**: Scope pre-check before planning — Add 7-principle scan of SCOPE.md to primary agent's Phase 1 (before spawning planning swarm); zero new agents needed [Source: 04]
- [ ] **ACT-011**: Agent self-check block for planning agents — Add self-validation against alignment criteria to specification, pseudocode, ndp-architect prompts (~200 tokens each) [Source: 04]
- [ ] **ACT-012**: Per-wave acceptance check — Add Step 3c.5 to `implementation-protocol.md`; scrum-master maps completed tasks to acceptance criteria and flags gaps before next wave [Source: 04]
- [ ] **ACT-013**: Create `features.json` manifest — Machine-readable feature dependency graph at `product/features/features.json`; start with fe-003, fe-004, ops-005 [Source: 06]
- [ ] **ACT-014**: Add `cargo audit` to /validate Tier 2 — Install `cargo-audit` in devcontainer; one-line addition to validate skill [Source: 06]
- [ ] **ACT-015**: Feature index generator — Script reads `product/features/*/SCOPE.md` and generates a table of feature ID, version, status, deps, GH Issue; add as `just feature-index` [Source: 06]
- [ ] **ACT-016**: Stale reference scanner — Script checks agent defs and protocol files for references to deprecated patterns (IDs 29, 32), removed files, old version numbers; add as `just stale-check` [Source: 06]
- [ ] **ACT-017**: Branch-per-feature convention — Document in `implementation-protocol.md`: implementation swarms create `feature/{phase}-{NNN}` branches, merge to main only after `/validate` passes [Source: 06, 02]
- [ ] **ACT-018**: Batch reflexion format — Update reflexion skill SKILL.md to accept array of `{pattern_id, reward, critique}` entries in one invocation [Source: 06]
- [ ] **ACT-019**: Run `claude-flow security scan` and fix detected hardcoded secret — Scan found 1 HIGH severity issue in `product/generic-platfo...` [Source: 05]
- [ ] **ACT-020**: Integrate claims system into swarm protocol — Add `claims_claim` before file edits, `claims_release` after, `claims_handoff` at SPARC phase transitions; already fully functional [Source: 05, 02]

## P1 — Next 2 Weeks (medium effort, clear value)

- [ ] **ACT-030**: Partial failure recovery protocol — When N of M agents fail, commit successful results, create targeted re-spawn prompts for failures, decrement drift budget [Source: 01]
- [ ] **ACT-031**: Create NDP-specific `specification` and `pseudocode` agent definitions — Currently referenced in planning spawns but fall back to generic agents without NDP domain knowledge [Source: 01]
- [ ] **ACT-032**: Implementation brief quality gate — After brief generation (Step 3f), verify all ADR pattern IDs resolve, all file paths exist, all SCOPE.md acceptance criteria are addressed [Source: 01, 04]
- [ ] **ACT-033**: Standardize ADR heading levels — Choose `## ADR-NNN:` with `### Context/Decision/Consequences` and update ndp-architect.md to match planning-protocol.md [Source: 01]
- [ ] **ACT-034**: Update stale technology status in ndp-architect.md — Silver is no longer "Planned" (it is implemented); Gold DDL generation exists [Source: 01]
- [ ] **ACT-035**: Add namespace isolation for concurrent swarms — Prefix all claude-flow memory keys with swarm instance ID (e.g., `swarm-{uuid}-{feature-id}-context`) [Source: 01, 02]
- [ ] **ACT-036**: Install and configure cargo-nextest — Add to devcontainer setup; create `.config/nextest.toml` with retry policy for flaky tests; update `/validate` to use nextest [Source: 03]
- [ ] **ACT-037**: Integration test runner script — `just integration` recipe: start docker-compose, run `cargo test -- --ignored`, capture results, tear down; usable by agents and humans [Source: 03]
- [ ] **ACT-038**: Coverage reporting with cargo-llvm-cov — Configure for current workspace; generate HTML + LCOV reports; update coverage badge; create `just coverage` recipe [Source: 03]
- [ ] **ACT-039**: GitHub Actions CI workflow — On PR to main: `cargo build --workspace`, `cargo test --workspace`, `cargo clippy`, anti-stub scan [Source: 03]
- [ ] **ACT-040**: Pre-push hook — Run `just check` (build + test + clippy) before push; blocking for main/release branches, warning-only for feature branches [Source: 03]
- [ ] **ACT-041**: Conventional commit enforcement — Install commit-msg hook validating `type(scope): description` format [Source: 03]
- [ ] **ACT-042**: ACCEPTANCE-MAP.md generation — Add to specification agent output; machine-readable mapping of AC-IDs to proposed test function names [Source: 04]
- [ ] **ACT-043**: Acceptance criteria gate at completion — Add Step 3e.5 to `implementation-protocol.md`; verify all ACs are covered by tests before marking feature complete [Source: 04]
- [ ] **ACT-044**: Progressive confidence reporting — Scrum-master reports confidence percentage and checkpoint status to GH Issue after each phase transition [Source: 04]
- [ ] **ACT-045**: Update ndp-scrum-master to accept swarm-ID parameter — Prefix all memory keys with swarm-ID for concurrent swarm support [Source: 02]
- [ ] **ACT-046**: Add claims_claim step for shared files in implementation protocol — Agents must claim before editing Cargo.toml, docker-compose, deploy.sh, shared mod.rs [Source: 02]
- [ ] **ACT-047**: Document safe swarm type combinations — Planning+Planning: safe; Planning+Implementation: safe; Implementation+Implementation: unsafe (shared Cargo.toml/lock) [Source: 02]
- [ ] **ACT-048**: Agent decision journal — Design decision log schema stored in `decisions` namespace; each agent logs what it chose, alternatives rejected, patterns consulted [Source: 06]
- [ ] **ACT-049**: Pattern supersession metadata — Add `supersedes` field to `save-pattern`; `get-pattern` warns on superseded results [Source: 06]
- [ ] **ACT-050**: Create ndp-security-auditor agent — Narrow scope: dep audit, secret scanning, privacy verification; conditional spawn when Cargo.toml or network-facing files modified [Source: 01, 06]
- [ ] **ACT-051**: CHANGELOG generation from conventional commits — Set up `git-cliff` or equivalent; generate Keep-a-Changelog format; human edits before release [Source: 06]
- [ ] **ACT-052**: Release automation skill — `/release` skill: CHANGELOG generation, manifest creation from workspace Cargo.toml, annotated tag creation, 3-artifact validation [Source: 06]
- [ ] **ACT-053**: deploy.sh rollback command — `deploy.sh rollback` redeploys previous manifest's Docker image; requires keeping previous image tagged + `previous_version` field in manifest [Source: 06]
- [ ] **ACT-054**: Add inter-swarm namespace for dependency signaling — Dedicated `inter-swarm` memory namespace for cross-swarm dependency messages [Source: 02]
- [ ] **ACT-055**: Pre-spawn baseline check — Run `cargo build --workspace` before scrum-master spawns agents to ensure workspace compiles [Source: 03, 06]
- [ ] **ACT-056**: Pattern conflict scanner — Weekly job via `/learner` extension; detect patterns in same taskType with divergent approaches; flag for human review [Source: 06]
- [ ] **ACT-057**: Cost awareness in scrum-master return — Add "Estimated agent count: N, waves: M, model tier: Opus/Haiku" to return template [Source: 06]

## P2 — Next Month (larger effort, strategic)

- [ ] **ACT-060**: Sub-coordinator concept — For 10+ agent swarms, scrum-master delegates wave coordination to `wave-coordinator` sub-agents; reduces context window pressure [Source: 01]
- [ ] **ACT-061**: Context window budget tracking — Coordinator estimates token usage (~500/prompt, ~1000/result, ~2000/overhead); if exceeding 80% of context, auto-split into sub-waves [Source: 01]
- [ ] **ACT-062**: Create ndp-deploy-engineer agent — Specializes in deploy.sh, Docker on Pi, git-as-transport, declarative manifests, integration environment [Source: 01]
- [ ] **ACT-063**: Create ndp-config-engineer agent — Specializes in config lifecycle: etcd, YAML hierarchy, stream registry, config-driven behavior edge cases [Source: 01]
- [ ] **ACT-064**: Prune unreferenced skills — Only 7 of 44 skills referenced by NDP protocols; archive or deprecate remaining ~37 to reduce agent cognitive load [Source: 01]
- [ ] **ACT-065**: Enable per-agent reflexion — Modify agent prompts to include reflexion step before returning; each sub-agent records its own pattern usage and effectiveness [Source: 01, 06]
- [ ] **ACT-066**: File conflict detection in implementation protocol — Before spawning, partition file modification list so no two agents edit same file; designate "owner" if overlap unavoidable [Source: 01]
- [ ] **ACT-067**: Document rollback procedure — When validation fails after 2 iterations: git revert, post failure to GH Issue, create new issue for fix, return context to user [Source: 01]
- [ ] **ACT-068**: Lightweight mid-phase /align-lite skill — Subset of vision guardian checks (technical constraints + scope alignment only); ~30 seconds per invocation [Source: 04]
- [ ] **ACT-069**: Implementation agent self-check — Extend self-check to ndp-rust-dev and ndp-tester: `cargo build` + `cargo test` on own crate before returning [Source: 04]
- [ ] **ACT-070**: AC verification script — `just ac-check {feature-id}` parses ACCEPTANCE-MAP.md, cross-references with `cargo test --list`, reports coverage [Source: 04]
- [ ] **ACT-071**: Drift detection post-edit hook — Check every file modification against brief's file list; warn if file not in brief [Source: 04]
- [ ] **ACT-072**: Confidence regression alert — If progressive confidence decreases between phases, scrum-master pauses and reports to user before proceeding [Source: 04]
- [ ] **ACT-073**: Delete verification-quality SKILL.md — The 650-line skill doc describes `truth` and `verify` CLI commands that do not exist; it is pure fiction and misleads agents [Source: 05]
- [ ] **ACT-074**: Performance benchmark framework — Create `benches/` with criterion benchmarks: WAL write throughput, Parquet read, Silver ETL batch, embedding generation [Source: 03]
- [ ] **ACT-075**: ARM64 cross-compilation CI — GitHub Actions workflow cross-compiling for `aarch64-unknown-linux-gnu` using `cross` or Docker buildx [Source: 03]
- [ ] **ACT-076**: Synthetic load generator — Tool generating N streams at M events/sec via MQTT against integration env; measures end-to-end latency, memory growth, disk usage [Source: 03]
- [ ] **ACT-077**: Automated regression detection — Store nextest JUnit XML per commit; compare against previous run; flag new failures, missing tests, perf regressions [Source: 03]
- [ ] **ACT-078**: Swarm checkpointing — After each wave, coordinator writes checkpoint to memory; new coordinator can resume from checkpoint on crash/context exhaustion [Source: 06]
- [ ] **ACT-079**: Auto-MEMORY.md sync skill — Reads AgentDB stats, GH Issue state, git tags, test counts; updates MEMORY.md sections; human reviews and approves [Source: 06]
- [ ] **ACT-080**: Cost estimation layer — Post-task hook estimates tokens (input + output) per agent; stores per-swarm cost; weekly summary [Source: 06]
- [ ] **ACT-081**: Stale claim recovery protocol — Coordinator checks claims_board every 5 min; claims with no progress for 10+ min marked stealable with reason "stale" [Source: 05]
- [ ] **ACT-082**: Post-commit hook for agent commits — Auto-run `cargo fmt --check` + `cargo clippy` after agent creates a commit [Source: 03]
- [ ] **ACT-083**: Concurrent swarm Phase 1 proof-of-concept — Run two swarms manually with namespaced memory; verify isolation and no state corruption [Source: 02]
- [ ] **ACT-084**: Feature dependency validation in scrum-master — Read `features.json` before spawning impl swarm; warn if `depends_on` features are not `status: "complete"` [Source: 06]
- [ ] **ACT-085**: Architecture diagram generation — Generate Mermaid diagram from Cargo.toml workspace members showing crate dependency graph [Source: 06]
- [ ] **ACT-086**: Agent creation guide: add routing registration step — Guide does not address how to register a new agent with `agent-routing.md` [Source: 01]
- [ ] **ACT-087**: Add memory TTL to coordination state — Set explicit TTL on `memory_store` calls to prevent stale state persisting after session crash [Source: 01]
- [ ] **ACT-088**: Coverage delta check on agent changes — Run `cargo-llvm-cov` on modified crates only; report delta (no threshold enforcement initially) [Source: 03]

## P3 — Backlog (research needed, future consideration)

- [ ] **ACT-090**: Multi-Hive concurrent swarms (Phase 3) — Patch `hive-mind_init` for swarmId, swarm registry, priority-based preemption, swarm dashboard, session save/restore [Source: 02]
- [ ] **ACT-091**: Multi-implementation concurrent swarms (Phase 2) — Claims-based file locking, crate-level ownership map, staggered cargo build, branch-per-swarm git workflow [Source: 02]
- [ ] **ACT-092**: Agent-driven test generation — When ndp-rust-dev creates a new module, auto-spawn ndp-tester to generate test cases from trait signatures and doc comments [Source: 03]
- [ ] **ACT-093**: Integration test isolation — Each integration test gets own TimescaleDB schema/database for parallel execution [Source: 03]
- [ ] **ACT-094**: Multi-human session coordination — Extend swarm-ID isolation to support multiple concurrent humans; session registry, conflict detection [Source: 06]
- [ ] **ACT-095**: Autonomous pattern lifecycle — Patterns auto-deprecate when avg reflexion reward < 0.3 in last 10 uses; auto-prune old superseded patterns; quarterly human review [Source: 06]
- [ ] **ACT-096**: Predictive cost budgeting — From historical token data, estimate cost before swarm launch; budget alerts; model tier auto-downgrade when budget is tight [Source: 06]
- [ ] **ACT-097**: Self-healing swarm orchestrator — Full checkpoint/resume; auto-recover from agent failures, context exhaustion, MCP outages [Source: 06]
- [ ] **ACT-098**: Investigate truth anchors via guidance — Check if `claude-flow guidance compile` produces useful output from CLAUDE.md for formalizing deprecated-approaches rules [Source: 05]
- [ ] **ACT-099**: Priority-based claim resolution — Higher-priority swarm wins claim conflicts; requires new logic in claims system [Source: 02]
- [ ] **ACT-100**: Agent testing framework — Guidance on how to validate that a new agent definition actually works before deploying it [Source: 01]
- [ ] **ACT-101**: Docker image scanning — Add Trivy or Grype scanning for Docker images built in deploy.sh [Source: 06]
- [ ] **ACT-102**: `cargo doc` recipe — `just docs` runs `cargo doc --workspace --no-deps`; generate on release [Source: 06]
- [ ] **ACT-103**: ndp-tester test directory structure update — Lines 39-46 reference `tests/components/redis_streams/` and `tests/orchestrator/` which may be outdated [Source: 01]
- [ ] **ACT-104**: Swarm-aware task_list filtering — Verify tag filtering on `task_list` works for swarm-scoped queries [Source: 02]
- [ ] **ACT-105**: MCP fallback coordination — If hive-mind/memory is unavailable, agents fall back to file-based coordination via `product/features/{id}/swarm-state.json` [Source: 01, 06]
