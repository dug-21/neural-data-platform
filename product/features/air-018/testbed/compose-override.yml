# BUG-007 testbed overrides
# No special overrides needed — standard smoke configuration
services:
  ndp-intelligence:
    environment:
      - INTELLIGENCE_WARMUP_THRESHOLD=5
      - INTELLIGENCE_POLL_INTERVAL_SECS=30
