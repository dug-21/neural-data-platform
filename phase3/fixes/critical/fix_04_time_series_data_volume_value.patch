# Fix 4: Add missing volume_value field to TimeSeriesData
# Error: no field `volume_value` on type `&data::TimeSeriesData`
# Location: src/features/realtime_pipeline.rs:53

--- a/src/data/mod.rs
+++ b/src/data/mod.rs
@@ -28,6 +28,8 @@ pub struct TimeSeriesData {
     pub low: f64,
     pub close: f64,
     pub volume: Vec<f64>, // Changed to Vec for compatibility
+    #[serde(default)]
+    pub volume_value: f64, // Single volume value for compatibility
     pub indicators: HashMap<String, f64>,
     // Storage compatibility fields
     pub source: Option<String>,